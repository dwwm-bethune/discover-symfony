{% extends 'base.html.twig' %}

{% block body %}
    {% for product in products %}
        <a href="{{ path('app_phone_show', { id: product.id }) }}">
            {{ product.name }}
        </a>
    {% endfor %}
{% endblock %}
